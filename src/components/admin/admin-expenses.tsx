"use client";
import { useTransition } from "react";
import { deleteExpenseAsAdmin } from "@/actions/admin";
import { Button } from "@/components/ui/button";
type Expense = { id: string; title: string; amount: number; type: "EXPENSE" | "INCOME"; expenseDate: Date; user: { email: string; name: string | null }; category: { name: string; icon: string | null } | null };
export function AdminExpenses({ expenses }: { expenses: Expense[] }) { const [pending, startTransition] = useTransition(); return <div className="overflow-x-auto rounded-xl border bg-card"><table className="w-full text-sm"><thead className="border-b text-left text-muted-foreground"><tr><th className="p-3">Transaction</th><th className="p-3">User</th><th className="p-3">Date</th><th className="p-3">Amount</th><th className="p-3" /></tr></thead><tbody>{expenses.map((expense) => <tr key={expense.id} className="border-b last:border-0"><td className="p-3"><b>{expense.category?.icon} {expense.title}</b><p className="text-xs text-muted-foreground">{expense.category?.name ?? "Uncategorized"}</p></td><td className="p-3">{expense.user.email}</td><td className="p-3">{new Date(expense.expenseDate).toLocaleDateString("id-ID")}</td><td className={expense.type === "INCOME" ? "p-3 text-emerald-600" : "p-3"}>{expense.type === "INCOME" ? "+" : "-"}{new Intl.NumberFormat("id-ID", { style: "currency", currency: "IDR", maximumFractionDigits: 0 }).format(expense.amount)}</td><td className="p-3"><Button size="sm" variant="ghost" disabled={pending} className="text-destructive" onClick={() => { if (window.confirm(`Delete ${expense.title}?`)) startTransition(() => { void deleteExpenseAsAdmin(expense.id); }); }}>Delete</Button></td></tr>)}</tbody></table></div>; }
