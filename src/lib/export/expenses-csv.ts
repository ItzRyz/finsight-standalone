export type ExpenseCsvRow={id:string;title:string;amount:number;type:"EXPENSE"|"INCOME";category:string;date:Date|string;merchant:string|null;location:string|null;description:string|null;receiptUrl:string|null};
function escapeCsvValue(value:unknown){if(value===null||value===undefined)return "";const v=String(value);return v.includes(",")||v.includes('"')||v.includes("\n")||v.includes("\r")?`"${v.replaceAll('"','""')}"`:v;}
function formatDate(value:Date|string){const d=new Date(value);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`;}
export function expensesToCsv(expenses:ExpenseCsvRow[]){const income=expenses.filter(x=>x.type==="INCOME").reduce((s,x)=>s+Number(x.amount),0);const expense=expenses.filter(x=>x.type==="EXPENSE").reduce((s,x)=>s+Number(x.amount),0);const headers=["Date","Title","Type","Category","Amount","Merchant","Location","Description","Receipt"];const rows=expenses.map(x=>[formatDate(x.date),x.title,x.type==="INCOME"?"Income":"Expense",x.category,x.amount,x.merchant??"",x.location??"",x.description??"",x.receiptUrl?"Yes":"No"]);const summary=[["FinSight Transaction Export"],[],["Total Transactions",expenses.length],["Total Income",income],["Total Expense",expense],["Balance",income-expense],[]];return ["\uFEFF",...summary.map(r=>r.map(escapeCsvValue).join(",")),headers.map(escapeCsvValue).join(","),...rows.map(r=>r.map(escapeCsvValue).join(","))].join("\n");}
export function downloadCsv(csv:string,filename:string){const blob=new Blob([csv],{type:"text/csv;charset=utf-8;"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);}
